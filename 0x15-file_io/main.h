#ifndef MAIN_H
#define MAIN_H

#include <sys/type.h>
#include <sys/stat.h>
#include <fcntl.h>
#include <unistd.h>

ssize_t read_textfile(const char *filename, size_t letters) :
	int create_file(cont char *filename, char *text_content) :
		int append_text_to_file(cont char *filname, char *text_content) :

#endif
